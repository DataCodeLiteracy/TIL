type DiceRollFn = (sides: number) => number;
const rollDice: DiceRollFn = (sides) => {
  /* COMPRESS */ return 0; /* END */
};

export default {};

/**
 * 요약
 *
 * 함수를 타입으로 등록하고 그걸 사용할 때 추론이 자동으로 되는..
 */
